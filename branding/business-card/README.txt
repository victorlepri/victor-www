
PRINTER SIZE VARIATIONS
========================

The folders standard-size, moo-size, and square-size are print variations of the deep thread card, each matched to the supplied InDesign guide. Each folder carries a front SVG, back SVG, front PNG, back PNG, two page print PDF, source template, and size notes.
